#include <stdio.h>

int main()  {
    int array1[10] = {0}, array2[10] = {0}, i, j;
    
    for(i = 0; i < 10; i++) {
        
        printf("ME INFORME UM NUMERO DO VETOR 1: ");
        scanf("%d", &array1[i]);
        
        
        array2[i] = array1[i];
    }
    
   for(i = 0; i < 10; i++) {
        for(j = 0;j < i; j++) {
            if(array2[i] == array2[j] && array2[i] != 0)
            array2[i] = 0;
        }
    }
    
    for(i = 0; i < 10; i++) {
        
        printf("%d", array2[i]);
    }
    

    return 0;
}
