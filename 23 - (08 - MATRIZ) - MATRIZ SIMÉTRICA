#include <stdio.h>

int main()  {
    int i, j, matriz[3][3] = {0}, matriz2[3][3] = {0}, maior = 0, linha[4] = {0}, coluna[4] = {0}, certo = 0;
    
    for(i = 0; i < 3; i++) {
        for(j = 0; j < 3; j++) {
            printf("ME INFORME O NUMERO DA LINHA %d COLUNA %d: ", i + 1, j + 1);
            scanf("%d", &matriz[i][j]);
        }
    }
    
    for(i = 0; i < 3; i++) {
        for(j = 0; j < 3; j++) {
            printf("%d\t", matriz[i][j]);
            if(matriz[j][i] == matriz[i][j])
            certo++;
        }
        printf("\n");
    }
    printf("\n\n");
    
    
    if(certo == 9)
        printf("MATRIZ SIMETRICA");
    else
        printf("MATRIZ NAO SIMETRICA");
    
    return 0;
}
